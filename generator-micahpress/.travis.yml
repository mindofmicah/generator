language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-micahpress' ]; then cd .. && eval "mv $currentfolder generator-micahpress" && cd generator-micahpress; fi

